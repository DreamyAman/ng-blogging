import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  imports: [RouterLink],
  selector: 'app-redirect-auth',
  styleUrl: './redirect-auth.css',
  templateUrl: './redirect-auth.html',
})
export class RedirectAuth {}
